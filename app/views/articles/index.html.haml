%h1 Listing all the articles
%p
  = link_to "Create new article", new_article_path

%table
  %tr
    %th Title
    %th Description
  - @articles.each do |art|
    %tr
      %td= art.title
      %td= art.description
      %td= link_to "Edit", edit_article_path(art)
      %td= link_to "Show", article_path(art)
      %td= link_to "Delete", article_path(art), method: :delete, data: {confirm: "Are you sure?"}
